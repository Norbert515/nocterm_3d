name: nocterm_3d
description: A declarative 3D renderer for terminal using Nocterm
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  nocterm: ^0.3.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
